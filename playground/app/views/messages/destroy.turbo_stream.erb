<%# For the requester, remove immediately via Turbo Stream response. %>
<%# All other subscribers receive the removal via Turbo broadcast (Message#broadcast_remove). %>
<%= turbo_stream.remove @message %>
