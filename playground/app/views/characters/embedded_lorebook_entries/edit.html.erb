<% content_for :title, @entry.display_name %>

<div class="space-y-6">
  <!-- Breadcrumbs -->
  <div class="breadcrumbs text-sm">
    <ul>
      <li>
        <%= link_to characters_path do %>
          <span class="icon-[lucide--users] size-4 mr-1"></span>
          <%= t("characters.title", default: "Characters") %>
        <% end %>
      </li>
      <li>
        <%= link_to @character.name, edit_character_path(@character) %>
      </li>
      <li><%= @entry.display_name.truncate(30) %></li>
    </ul>
  </div>

  <!-- Header -->
  <div class="flex items-center justify-between">
    <div>
      <h1 class="text-2xl font-bold flex items-center gap-3">
        <%= @entry.display_name %>
        <% if @entry.enabled? %>
          <span class="badge badge-success badge-sm"><%= t("lorebook_entries.status.enabled", default: "Enabled") %></span>
        <% else %>
          <span class="badge badge-ghost badge-sm"><%= t("lorebook_entries.status.disabled", default: "Disabled") %></span>
        <% end %>
        <% if @entry.constant? %>
          <span class="badge badge-warning badge-sm"><%= t("lorebook_entries.status.constant", default: "Constant") %></span>
        <% end %>
      </h1>
      <p class="text-base-content/60 mt-1">
        <%= t("embedded_lorebook_entries.edit_description", default: "Edit entry in %{character}'s embedded lorebook") % { character: @character.name } %>
      </p>
    </div>
  </div>

  <!-- Form -->
  <div class="card bg-base-100 shadow-sm border border-base-300">
    <div class="card-body">
      <%= render "form", entry: @entry, character: @character, mode: :public %>
    </div>
  </div>

  <!-- Danger Zone -->
  <div class="card bg-base-100 shadow-sm border border-error/30">
    <div class="card-body">
      <h3 class="card-title text-error">
        <span class="icon-[lucide--alert-triangle] size-5"></span>
        <%= t("lorebook_entries.danger_zone", default: "Danger Zone") %>
      </h3>
      <p class="text-base-content/60 text-sm"><%= t("lorebook_entries.danger_description", default: "Once you delete an entry, there is no going back.") %></p>
      <div class="card-actions mt-4">
        <%= button_to character_embedded_lorebook_entry_path(@character, @entry.id),
            method: :delete,
            class: "btn btn-error btn-outline gap-2",
            data: { turbo_confirm: t("lorebook_entries.delete_confirm", default: "Delete this entry?") } do %>
          <span class="icon-[lucide--trash-2] size-4"></span>
          <%= t("lorebook_entries.delete", default: "Delete Entry") %>
        <% end %>
      </div>
    </div>
  </div>
</div>
