<% content_for :title, t("llm_providers.edit.title", name: @provider.name) %>

<div class="space-y-6">
  <!-- Breadcrumbs -->
  <div class="breadcrumbs text-sm">
    <ul>
      <li>
        <%= link_to settings_llm_providers_path do %>
          <span class="icon-[lucide--bot] size-4 mr-1"></span>
          <%= t("llm_providers.index.title") %>
        <% end %>
      </li>
      <li>
        <%= @provider.name %>
        <% if LLMProvider.get_default&.id == @provider.id %>
          <span class="badge badge-success badge-xs ml-2"><%= t("llm_providers.default_badge", default: "Default") %></span>
        <% end %>
      </li>
    </ul>
  </div>

  <!-- Form -->
  <div class="card bg-base-100 shadow-sm border border-base-300"
       data-controller="llm-settings"
       data-llm-settings-provider-id-value="<%= @provider.id %>">
    <div class="card-body">
      <%= render "form", provider: @provider %>

      <!-- Connection Status -->
      <div id="connection_status_<%= @provider.id %>" class="mt-4">
        <%= render "settings/llm_providers/connection_status", result: nil, provider: @provider %>
      </div>
    </div>
  </div>

  <!-- Danger Zone -->
  <div class="card bg-base-100 shadow-sm border border-error/30">
    <div class="card-body">
      <h3 class="card-title text-error"><%= t("llm_providers.edit.danger_zone") %></h3>
      <p class="text-base-content/60 text-sm"><%= t("llm_providers.edit.danger_description") %></p>
      <div class="card-actions mt-4">
        <%= button_to settings_llm_provider_path(@provider),
            method: :delete,
            class: "btn btn-error btn-outline gap-2",
            data: { turbo_confirm: t("llm_providers.destroy.confirm", name: @provider.name) } do %>
          <span class="icon-[lucide--trash-2] size-4"></span>
          <%= t("llm_providers.actions.delete") %>
        <% end %>
      </div>
    </div>
  </div>
</div>
