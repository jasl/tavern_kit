<%# Turbo Stream: Replace the entire conversation lorebooks frame after destroying %>
<%= turbo_stream.replace "conversation_lorebooks" do %>
  <%= render template: "conversations/lorebooks/index", locals: { conversation: @conversation } %>
<% end %>
