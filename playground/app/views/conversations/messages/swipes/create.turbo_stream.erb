<%# Update the message to reflect the new active swipe %>
<%= turbo_stream.replace @message do %>
  <%= render partial: "messages/message",
             locals: { message: @message, conversation: @conversation, space: @space } %>
<% end %>
